services:
  app:
    entrypoint: ["poetry", "run", "python", "app.py"]
    command: [
      "--read", "result1772042419.xml",
      "--students", "students.json",
      "--rooms", "rooms.json",
      "--output", "xml",
      "--query", "q2"
    ]
    build:
      context: .
      target: dev
    volumes:
      - python-resources:/code/resources/
    networks:
      - front-tier
      - back-tier
    depends_on:
      pgdb:
        condition: service_healthy
        restart: true
    develop:
      watch:
        - action: sync
          path: .
          target: .
          initial_sync: true
  pgdb:
    image: postgres
    restart: always
    volumes:
      - pg-data:/var/lib/postgresql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - back-tier
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: innowise
      POSTGRES_PASSWORD: innowise
      POSTGRES_DB: task1
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      retries: 5
      start_period: 30s
      timeout: 10s

volumes:
  pg-data: {}
  python-resources: {}
networks:
  front-tier: {}
  back-tier: {}


